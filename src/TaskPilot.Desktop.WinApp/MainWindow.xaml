<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="TaskPilot.Desktop.WinApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TaskPilot.Desktop.WinApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="TaskPilot">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="gridXamlRoot" Loaded="gridXamlRoot_Loaded">
        <Grid.Resources>
            <Style x:Key="NavFontIconStyle" TargetType="FontIcon">
                <Setter Property="FontFamily" Value="{StaticResource MaterialOutlineThinIconFontFamily}"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="RenderTransformOrigin" Value="0.5 0.5"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="2.1" ScaleY="2"/>
                    </Setter.Value>
                </Setter>
            </Style>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar x:Name="titleBar" 
                  Grid.Row="0" 
                  IsBackButtonVisible="False" 
                  Title="TaskPilot"
                  IsPaneToggleButtonVisible="True"
                  PaneToggleRequested="titleBar_PaneToggleRequested"/>

        <NavigationView x:Name="navView"
                        Grid.Row="1"
                        IsSettingsVisible="True"
                        IsBackButtonVisible="Visible"
                        IsPaneToggleButtonVisible="False"
                        SelectionChanged="navView_SelectionChanged"
                        AlwaysShowHeader="False"
                        Loaded="navView_Loaded">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Dashboard">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xe88a;" Style="{StaticResource NavFontIconStyle}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Projects" Tag="Projects">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xeb13;" Style="{StaticResource NavFontIconStyle}"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <Frame x:Name="navFrame" Loaded="navFrame_Loaded">
                <Frame.ContentTransitions>
                    <TransitionCollection>
                        <NavigationThemeTransition  />
                    </TransitionCollection>
                </Frame.ContentTransitions>
            </Frame>
        </NavigationView>
    </Grid>
</Window>
