<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TaskPilot.Desktop.WinApp.Pages.ProjectPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TaskPilot.Desktop.WinApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:entities="using:TaskPilot.Core.Components.Entities"
    xmlns:data="using:TaskPilot.Core.Components.Data"
    xmlns:viewmodel="using:TaskPilot.Core.ViewModel"
    xmlns:controls="using:TaskPilot.Desktop.WinApp.Controls"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodel:ProjectPageViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 0: Title -->
        <TextBlock Grid.Row="0" 
                   Margin="36,36,36,0"
                   VerticalAlignment="Stretch" 
                   MaxWidth="1200"
                   Style="{StaticResource TitleTextBlockStyle}" 
                   Text="{x:Bind ViewModel.Title, Mode=OneWay}"/>

        <!-- Row 1: Main Content -->
        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Disabled" 
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid MaxWidth="1200" Margin="24" RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <controls:CollapsibleDescription Grid.Row="0"
                                                     Text="{x:Bind ViewModel.CurrentProject.Description, Mode=OneWay}"/>

                    <Grid Grid.Row="1" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <AutoSuggestBox Grid.Column="0"
                                        PlaceholderText="Search tasks..."
                                        QueryIcon="Find"
                                        Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                        VerticalAlignment="Center"/>

                        <Button Grid.Column="1"
                                Content="Milestones"
                                Command="{x:Bind ViewModel.NavigateToMilestonesCommand}"/>

                        <Button Grid.Column="2"
                                Content="New Task"
                                Style="{StaticResource AccentButtonStyle}"
                                Command="{x:Bind ViewModel.CreateTaskCommand}">
                            <Button.KeyboardAccelerators>
                                <KeyboardAccelerator Modifiers="Control" Key="N"/>
                            </Button.KeyboardAccelerators>
                        </Button>
                    </Grid>

                    <!-- Tasks List Control -->
                    <controls:TasksListControl Grid.Row="2" 
                                               DataContext="{x:Bind ViewModel.TasksListViewModel}"
                                               ShowProject="False"
                                               ShowMilestone="True"/>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
