<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TaskPilot.Desktop.WinApp.Pages.ProjectFormPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TaskPilot.Desktop.WinApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodel="using:TaskPilot.Core.ViewModel"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodel:ProjectFormViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Margin="36,36,36,0"
                   VerticalAlignment="Stretch" MaxWidth="1200"
                   Style="{StaticResource TitleTextBlockStyle}" 
                   Text="{Binding Title, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>

        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Disabled" 
                      VerticalScrollBarVisibility="Auto">
            <Grid>
                <Grid MaxWidth="1200" Margin="24" RowSpacing="16" ColumnSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2.5*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox Grid.Column="0" Grid.Row="0" 
                             Header="Name"
                             PlaceholderText="Enter project name"
                             Text="{Binding ProjectName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <ProgressBar Grid.Column="0" Grid.Row="0" 
                                 IsIndeterminate="{Binding IsCheckingProjectName, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent"
                                 VerticalAlignment="Bottom" 
                                 Margin="3,0"
                                 BorderThickness="0"/>

                    <Grid Grid.Column="0" Grid.Row="1" ColumnSpacing="12" Margin="0,12,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" 
                                   Grid.ColumnSpan="2" 
                                   FontWeight="Normal"
                                   Text="Color"
                                   Margin="{ThemeResource TextBoxTopHeaderMargin}"/>

                        <TextBlock Grid.Row="1" 
                                   Grid.Column="0"
                                   Text="&#xe2c7;" 
                                   FontSize="80" 
                                   FontFamily="{StaticResource MaterialFilledThinIconFontFamily}">
                            <TextBlock.Foreground>
                                <SolidColorBrush Color="{Binding Color.HexValue, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource HexToColorConverter}}"/>
                            </TextBlock.Foreground>
                        </TextBlock>

                        <GridView Grid.Row="1" 
                                  Grid.Column="1"
                                  ItemsSource="{Binding Colors, Mode=OneWay}"
                                  SelectedItem="{Binding Color, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <GridView.ItemTemplate>
                                <DataTemplate>
                                    <Border Width="24" Height="24" CornerRadius="4">
                                        <Border.Background>
                                            <SolidColorBrush Color="{Binding HexValue, Converter={StaticResource HexToColorConverter}}"/>
                                        </Border.Background>
                                    </Border>
                                </DataTemplate>
                            </GridView.ItemTemplate>
                            <GridView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid x:Name="MaxItemsWrapGrid"
                                                   MaximumRowsOrColumns="9"
                                                   Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </GridView.ItemsPanel>

                        </GridView>
                    </Grid>

                    <TextBox Grid.Column="0" Grid.Row="2" 
                             Header="Description"
                             PlaceholderText="Add a brief description..."
                             Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             MinHeight="80"
                             MaxHeight="350"/>

                    <Border Grid.Column="1" 
                            Grid.Row="0" 
                            Grid.RowSpan="5" 
                            BorderThickness="1" 
                            CornerRadius="5"
                            Margin="0,26,0,0"
                            BorderBrush="{ThemeResource ControlElevationBorderBrush}"
                            VerticalAlignment="Top">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" 
                                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                    BorderBrush="{ThemeResource ControlElevationBorderBrush}" 
                                    BorderThickness="0,0,0,1" 
                                    Padding="12">
                                <TextBlock Text="Review your changes" 
                                           FontSize="16"
                                           Style="{StaticResource SubtitleTextBlockStyle}"/>
                            </Border>

                            <Border Grid.Row="1"
                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                                <ItemsView ItemsSource="{Binding Popups, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                           SelectionMode="None">
                                    <ItemsView.ItemTemplate>
                                        <DataTemplate>
                                            <ItemContainer>
                                                <InfoBar IsOpen="True" IsClosable="False"
                                                         Title="{Binding Title}"
                                                         Message="{Binding Message}"
                                                         Severity="{Binding PopupType, Converter={StaticResource PopupTypeToSeverityConverter}}"/>
                                            </ItemContainer>
                                        </DataTemplate>
                                    </ItemsView.ItemTemplate>
                                </ItemsView>
                            </Border>
                        </Grid>
                    </Border>

                    <StackPanel Grid.Column="0" Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Save" Style="{StaticResource FormAccentButtonStyle}"
                                Command="{Binding SaveChangesCommand}"/>
                        <Button Content="Cancel" Margin="12,0,0,0" Style="{StaticResource FormButtonStyle}"
                                Command="{Binding CancelCommand}"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
